# Two separate free-text fields per booking

Each booking carries two free-text fields: one the member fills in (allergies, accessibility needs, practical wishes) and one internal admin-only note the member cannot see. Chosen so member-facing wishes and admin-internal handling stay separate.
